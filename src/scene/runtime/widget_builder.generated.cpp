// Generated by scripts/generate_scene_runtime_cpp.py. Do not edit by hand.

#include "tuinator/scene/detail/widget_builder.generated.hpp"

#include <tuinator/tuinator.hpp>
#include <tuinator/scene/scene_context.hpp>
#include <tuinator/scene/detail/json_node.hpp>
#include <tuinator/scene/detail/style_resolve.hpp>

#include <nlohmann/json.hpp>

#include <memory>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

namespace tuinator::scene::detail {

namespace {

#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-function"
#endif

#if defined(_MSC_VER)
#pragma warning(push)
#pragma warning(disable : 4100)
#pragma warning(disable : 4505)
#endif

void fill_BarChartOptions(tuinator::BarChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "customGlyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "custom_glyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "valueLabel")) {
        opts.value_label = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "value_label")) {
        opts.value_label = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "orientation")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Vertical") { opts.orientation = tuinator::BarChartOrientation::Vertical; }
            else if (_enum_val == "Horizontal") { opts.orientation = tuinator::BarChartOrientation::Horizontal; }
            else { opts.orientation = tuinator::BarChartOrientation::Vertical; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showValues")) {
        opts.show_values = json::as_bool(*value, opts.show_values);
    }
    if (const nlohmann::json* value = json::find(node, "show_values")) {
        opts.show_values = json::as_bool(*value, opts.show_values);
    }
    if (const nlohmann::json* value = json::find(node, "showAxis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "show_axis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLabels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_labels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "interactive")) {
        opts.interactive = json::as_bool(*value, opts.interactive);
    }
    if (const nlohmann::json* value = json::find(node, "barGap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "bar_gap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "valueStyle")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "value_style")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_BigTextOptions(tuinator::BigTextOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "kind")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Block") { opts.kind = tuinator::BigTextKind::Block; }
            else if (_enum_val == "Half") { opts.kind = tuinator::BigTextKind::Half; }
            else if (_enum_val == "Narrow") { opts.kind = tuinator::BigTextKind::Narrow; }
            else if (_enum_val == "Quadrant") { opts.kind = tuinator::BigTextKind::Quadrant; }
            else if (_enum_val == "Third") { opts.kind = tuinator::BigTextKind::Third; }
            else if (_enum_val == "Sextant") { opts.kind = tuinator::BigTextKind::Sextant; }
            else if (_enum_val == "Quarter") { opts.kind = tuinator::BigTextKind::Quarter; }
            else if (_enum_val == "Octant") { opts.kind = tuinator::BigTextKind::Octant; }
            else if (_enum_val == "Braille") { opts.kind = tuinator::BigTextKind::Braille; }
            else if (_enum_val == "Shade") { opts.kind = tuinator::BigTextKind::Shade; }
            else if (_enum_val == "Letter") { opts.kind = tuinator::BigTextKind::Letter; }
            else if (_enum_val == "Banner") { opts.kind = tuinator::BigTextKind::Banner; }
            else if (_enum_val == "Outline") { opts.kind = tuinator::BigTextKind::Outline; }
            else if (_enum_val == "Stacked") { opts.kind = tuinator::BigTextKind::Stacked; }
            else if (_enum_val == "Isometric") { opts.kind = tuinator::BigTextKind::Isometric; }
            else if (_enum_val == "Slant") { opts.kind = tuinator::BigTextKind::Slant; }
            else if (_enum_val == "Standard") { opts.kind = tuinator::BigTextKind::Standard; }
            else if (_enum_val == "Small") { opts.kind = tuinator::BigTextKind::Small; }
            else if (_enum_val == "Big") { opts.kind = tuinator::BigTextKind::Big; }
            else if (_enum_val == "Doom") { opts.kind = tuinator::BigTextKind::Doom; }
            else if (_enum_val == "FigletShadow") { opts.kind = tuinator::BigTextKind::FigletShadow; }
            else { opts.kind = tuinator::BigTextKind::Block; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "letterSpacing")) {
        opts.letter_spacing = json::as_int(*value, opts.letter_spacing);
    }
    if (const nlohmann::json* value = json::find(node, "letter_spacing")) {
        opts.letter_spacing = json::as_int(*value, opts.letter_spacing);
    }
    if (const nlohmann::json* value = json::find(node, "scale")) {
        opts.scale = json::as_int(*value, opts.scale);
    }
    if (const nlohmann::json* value = json::find(node, "rainbow")) {
        opts.rainbow = json::as_bool(*value, opts.rainbow);
    }
    if (const nlohmann::json* value = json::find(node, "underline")) {
        opts.underline = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "gradientAxis")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Vertical") { opts.gradient_axis = tuinator::BigTextGradientAxis::Vertical; }
            else if (_enum_val == "Horizontal") { opts.gradient_axis = tuinator::BigTextGradientAxis::Horizontal; }
            else { opts.gradient_axis = tuinator::BigTextGradientAxis::Vertical; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "gradient_axis")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Vertical") { opts.gradient_axis = tuinator::BigTextGradientAxis::Vertical; }
            else if (_enum_val == "Horizontal") { opts.gradient_axis = tuinator::BigTextGradientAxis::Horizontal; }
            else { opts.gradient_axis = tuinator::BigTextGradientAxis::Vertical; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "shadowStyle")) {
        opts.shadow_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "shadow_style")) {
        opts.shadow_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "shadowDx")) {
        opts.shadow_dx = json::as_int(*value, opts.shadow_dx);
    }
    if (const nlohmann::json* value = json::find(node, "shadow_dx")) {
        opts.shadow_dx = json::as_int(*value, opts.shadow_dx);
    }
    if (const nlohmann::json* value = json::find(node, "shadowDy")) {
        opts.shadow_dy = json::as_int(*value, opts.shadow_dy);
    }
    if (const nlohmann::json* value = json::find(node, "shadow_dy")) {
        opts.shadow_dy = json::as_int(*value, opts.shadow_dy);
    }
    if (const nlohmann::json* value = json::find(node, "shadowLayers")) {
        opts.shadow_layers = json::as_int(*value, opts.shadow_layers);
    }
    if (const nlohmann::json* value = json::find(node, "shadow_layers")) {
        opts.shadow_layers = json::as_int(*value, opts.shadow_layers);
    }
}

void fill_BoxOptions(tuinator::BoxOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "gap")) {
        opts.gap = json::as_int(*value, opts.gap);
    }
    if (const nlohmann::json* value = json::find(node, "padding")) {
        opts.padding = json::as_int(*value, opts.padding);
    }
}

void fill_CandlestickChartOptions(tuinator::CandlestickChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showAxis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "show_axis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLabels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_labels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "showVolume")) {
        opts.show_volume = json::as_bool(*value, opts.show_volume);
    }
    if (const nlohmann::json* value = json::find(node, "show_volume")) {
        opts.show_volume = json::as_bool(*value, opts.show_volume);
    }
    if (const nlohmann::json* value = json::find(node, "barWidth")) {
        opts.bar_width = json::as_int(*value, opts.bar_width);
    }
    if (const nlohmann::json* value = json::find(node, "bar_width")) {
        opts.bar_width = json::as_int(*value, opts.bar_width);
    }
    if (const nlohmann::json* value = json::find(node, "barGap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "bar_gap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "compactLayout")) {
        opts.compact_layout = json::as_bool(*value, opts.compact_layout);
    }
    if (const nlohmann::json* value = json::find(node, "compact_layout")) {
        opts.compact_layout = json::as_bool(*value, opts.compact_layout);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "upStyle")) {
        opts.up_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "up_style")) {
        opts.up_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "downStyle")) {
        opts.down_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "down_style")) {
        opts.down_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "wickStyle")) {
        opts.wick_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "wick_style")) {
        opts.wick_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_CheckboxGlyphs(tuinator::CheckboxGlyphs& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "unchecked")) {
        opts.unchecked = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "checked")) {
        opts.checked = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "gap")) {
        opts.gap = json::as_string(*value);
    }
}

void fill_CheckboxOptions(tuinator::CheckboxOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "highlightRowOnFocus")) {
        opts.highlight_row_on_focus = json::as_bool(*value, opts.highlight_row_on_focus);
    }
    if (const nlohmann::json* value = json::find(node, "highlight_row_on_focus")) {
        opts.highlight_row_on_focus = json::as_bool(*value, opts.highlight_row_on_focus);
    }
    if (const nlohmann::json* value = json::find(node, "markerStyle")) {
        opts.marker_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "marker_style")) {
        opts.marker_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "markerCheckedStyle")) {
        opts.marker_checked_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "marker_checked_style")) {
        opts.marker_checked_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "labelStyle")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "label_style")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "labelCheckedStyle")) {
        opts.label_checked_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "label_checked_style")) {
        opts.label_checked_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "focusedStyle")) {
        opts.focused_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "focused_style")) {
        opts.focused_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_GaugeChartOptions(tuinator::GaugeChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Arc") { opts.style = tuinator::GaugeStyle::Arc; }
            else if (_enum_val == "Semicircle") { opts.style = tuinator::GaugeStyle::Semicircle; }
            else if (_enum_val == "Horizontal") { opts.style = tuinator::GaugeStyle::Horizontal; }
            else { opts.style = tuinator::GaugeStyle::Arc; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyph")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.glyph = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.glyph = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.glyph = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.glyph = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.glyph = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.glyph = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.glyph = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.glyph = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.glyph = tuinator::ChartGlyphStyle::Custom; }
            else { opts.glyph = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "unit")) {
        opts.unit = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showValue")) {
        opts.show_value = json::as_bool(*value, opts.show_value);
    }
    if (const nlohmann::json* value = json::find(node, "show_value")) {
        opts.show_value = json::as_bool(*value, opts.show_value);
    }
    if (const nlohmann::json* value = json::find(node, "showTicks")) {
        opts.show_ticks = json::as_bool(*value, opts.show_ticks);
    }
    if (const nlohmann::json* value = json::find(node, "show_ticks")) {
        opts.show_ticks = json::as_bool(*value, opts.show_ticks);
    }
    if (const nlohmann::json* value = json::find(node, "diameter")) {
        opts.diameter = json::as_int(*value, opts.diameter);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "trackStyle")) {
        opts.track_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "track_style")) {
        opts.track_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "fillStyle")) {
        opts.fill_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "fill_style")) {
        opts.fill_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "valueStyle")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "value_style")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "tickStyle")) {
        opts.tick_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "tick_style")) {
        opts.tick_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_GridOptions(tuinator::GridOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "columns")) {
        opts.columns = json::as_int(*value, opts.columns);
    }
    if (const nlohmann::json* value = json::find(node, "gap")) {
        opts.gap = json::as_int(*value, opts.gap);
    }
    if (const nlohmann::json* value = json::find(node, "padding")) {
        opts.padding = json::as_int(*value, opts.padding);
    }
}

void fill_HeatmapOptions(tuinator::HeatmapOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showRowLabels")) {
        opts.show_row_labels = json::as_bool(*value, opts.show_row_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_row_labels")) {
        opts.show_row_labels = json::as_bool(*value, opts.show_row_labels);
    }
    if (const nlohmann::json* value = json::find(node, "showColLabels")) {
        opts.show_col_labels = json::as_bool(*value, opts.show_col_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_col_labels")) {
        opts.show_col_labels = json::as_bool(*value, opts.show_col_labels);
    }
    if (const nlohmann::json* value = json::find(node, "showLegend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "show_legend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "cellGap")) {
        opts.cell_gap = json::as_int(*value, opts.cell_gap);
    }
    if (const nlohmann::json* value = json::find(node, "cell_gap")) {
        opts.cell_gap = json::as_int(*value, opts.cell_gap);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "labelStyle")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "label_style")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "lowStyle")) {
        opts.low_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "low_style")) {
        opts.low_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "highStyle")) {
        opts.high_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "high_style")) {
        opts.high_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_HistogramOptions(tuinator::HistogramOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "customGlyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "custom_glyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showAxis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "show_axis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLabels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_labels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "showCounts")) {
        opts.show_counts = json::as_bool(*value, opts.show_counts);
    }
    if (const nlohmann::json* value = json::find(node, "show_counts")) {
        opts.show_counts = json::as_bool(*value, opts.show_counts);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "countStyle")) {
        opts.count_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "count_style")) {
        opts.count_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_LineChartOptions(tuinator::LineChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "customGlyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "custom_glyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "mode")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Line") { opts.mode = tuinator::LineChartMode::Line; }
            else if (_enum_val == "Area") { opts.mode = tuinator::LineChartMode::Area; }
            else if (_enum_val == "Sparkline") { opts.mode = tuinator::LineChartMode::Sparkline; }
            else if (_enum_val == "Scatter") { opts.mode = tuinator::LineChartMode::Scatter; }
            else if (_enum_val == "Step") { opts.mode = tuinator::LineChartMode::Step; }
            else { opts.mode = tuinator::LineChartMode::Line; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showAxes")) {
        opts.show_axes = json::as_bool(*value, opts.show_axes);
    }
    if (const nlohmann::json* value = json::find(node, "show_axes")) {
        opts.show_axes = json::as_bool(*value, opts.show_axes);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLegend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "show_legend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "mirror")) {
        opts.mirror = json::as_bool(*value, opts.mirror);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legendStyle")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legend_style")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_NerdGlyphMatrixGalleryStyle(tuinator::NerdGlyphMatrixGalleryStyle& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "background")) {
        opts.background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "domainHeader")) {
        opts.domain_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "domain_header")) {
        opts.domain_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "categoryHeader")) {
        opts.category_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "category_header")) {
        opts.category_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "glyph")) {
        opts.glyph = style::resolve_style(*value, ctx.theme);
    }
}

void fill_NerdIconCatalogGalleryStyle(tuinator::NerdIconCatalogGalleryStyle& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "background")) {
        opts.background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "domainHeader")) {
        opts.domain_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "domain_header")) {
        opts.domain_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "categoryHeader")) {
        opts.category_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "category_header")) {
        opts.category_header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "rowText")) {
        opts.row_text = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "row_text")) {
        opts.row_text = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "icon")) {
        opts.icon = style::resolve_style(*value, ctx.theme);
    }
}

void fill_PieChartOptions(tuinator::PieChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::PieChartStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::PieChartStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::PieChartStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::PieChartStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::PieChartStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::PieChartStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::PieChartStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::PieChartStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::PieChartStyle::Custom; }
            else { opts.style = tuinator::PieChartStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "customGlyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "custom_glyph")) {
        opts.custom_glyph = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "showLegend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "show_legend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "showPercent")) {
        opts.show_percent = json::as_bool(*value, opts.show_percent);
    }
    if (const nlohmann::json* value = json::find(node, "show_percent")) {
        opts.show_percent = json::as_bool(*value, opts.show_percent);
    }
    if (const nlohmann::json* value = json::find(node, "interactive")) {
        opts.interactive = json::as_bool(*value, opts.interactive);
    }
    if (const nlohmann::json* value = json::find(node, "rotation")) {
        opts.rotation = json::as_double(*value, opts.rotation);
    }
    if (const nlohmann::json* value = json::find(node, "diameter")) {
        opts.diameter = json::as_int(*value, opts.diameter);
    }
    if (const nlohmann::json* value = json::find(node, "minSliceRatio")) {
        opts.min_slice_ratio = json::as_double(*value, opts.min_slice_ratio);
    }
    if (const nlohmann::json* value = json::find(node, "min_slice_ratio")) {
        opts.min_slice_ratio = json::as_double(*value, opts.min_slice_ratio);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legendStyle")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legend_style")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_ProgressBarGlyphs(tuinator::ProgressBarGlyphs& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "fill")) {
        opts.fill = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "head")) {
        opts.head = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "empty")) {
        opts.empty = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "leftCap")) {
        opts.left_cap = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "left_cap")) {
        opts.left_cap = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "rightCap")) {
        opts.right_cap = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "right_cap")) {
        opts.right_cap = json::as_string(*value);
    }
}

void fill_ProgressBarOptions(tuinator::ProgressBarOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "layout")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Plain") { opts.layout = tuinator::ProgressBarLayout::Plain; }
            else if (_enum_val == "Bracketed") { opts.layout = tuinator::ProgressBarLayout::Bracketed; }
            else if (_enum_val == "Blocks") { opts.layout = tuinator::ProgressBarLayout::Blocks; }
            else if (_enum_val == "Dots") { opts.layout = tuinator::ProgressBarLayout::Dots; }
            else if (_enum_val == "Pill") { opts.layout = tuinator::ProgressBarLayout::Pill; }
            else if (_enum_val == "Labeled") { opts.layout = tuinator::ProgressBarLayout::Labeled; }
            else if (_enum_val == "FilledLabel") { opts.layout = tuinator::ProgressBarLayout::FilledLabel; }
            else if (_enum_val == "Tqdm") { opts.layout = tuinator::ProgressBarLayout::Tqdm; }
            else if (_enum_val == "BrailleMetric") { opts.layout = tuinator::ProgressBarLayout::BrailleMetric; }
            else if (_enum_val == "BrailleWave") { opts.layout = tuinator::ProgressBarLayout::BrailleWave; }
            else if (_enum_val == "Pulse") { opts.layout = tuinator::ProgressBarLayout::Pulse; }
            else if (_enum_val == "Shimmer") { opts.layout = tuinator::ProgressBarLayout::Shimmer; }
            else if (_enum_val == "Bounce") { opts.layout = tuinator::ProgressBarLayout::Bounce; }
            else if (_enum_val == "SlideBlock") { opts.layout = tuinator::ProgressBarLayout::SlideBlock; }
            else if (_enum_val == "MovingDot") { opts.layout = tuinator::ProgressBarLayout::MovingDot; }
            else if (_enum_val == "TaskRow") { opts.layout = tuinator::ProgressBarLayout::TaskRow; }
            else { opts.layout = tuinator::ProgressBarLayout::Plain; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "label")) {
        opts.label = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "insideLabel")) {
        opts.inside_label = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "inside_label")) {
        opts.inside_label = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "metricValue")) {
        opts.metric_value = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "metric_value")) {
        opts.metric_value = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "showPercent")) {
        opts.show_percent = json::as_bool(*value, opts.show_percent);
    }
    if (const nlohmann::json* value = json::find(node, "show_percent")) {
        opts.show_percent = json::as_bool(*value, opts.show_percent);
    }
    if (const nlohmann::json* value = json::find(node, "percentPosition")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "None") { opts.percent_position = tuinator::ProgressBarPercentPosition::None; }
            else if (_enum_val == "Right") { opts.percent_position = tuinator::ProgressBarPercentPosition::Right; }
            else if (_enum_val == "Inside") { opts.percent_position = tuinator::ProgressBarPercentPosition::Inside; }
            else { opts.percent_position = tuinator::ProgressBarPercentPosition::None; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "percent_position")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "None") { opts.percent_position = tuinator::ProgressBarPercentPosition::None; }
            else if (_enum_val == "Right") { opts.percent_position = tuinator::ProgressBarPercentPosition::Right; }
            else if (_enum_val == "Inside") { opts.percent_position = tuinator::ProgressBarPercentPosition::Inside; }
            else { opts.percent_position = tuinator::ProgressBarPercentPosition::None; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "segmented")) {
        opts.segmented = json::as_bool(*value, opts.segmented);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "animationPhase")) {
        opts.animation_phase = json::as_int(*value, opts.animation_phase);
    }
    if (const nlohmann::json* value = json::find(node, "animation_phase")) {
        opts.animation_phase = json::as_int(*value, opts.animation_phase);
    }
    if (const nlohmann::json* value = json::find(node, "pulseWidth")) {
        opts.pulse_width = json::as_int(*value, opts.pulse_width);
    }
    if (const nlohmann::json* value = json::find(node, "pulse_width")) {
        opts.pulse_width = json::as_int(*value, opts.pulse_width);
    }
    if (const nlohmann::json* value = json::find(node, "indeterminate")) {
        opts.indeterminate = json::as_bool(*value, opts.indeterminate);
    }
    if (const nlohmann::json* value = json::find(node, "completed")) {
        opts.completed = json::as_bool(*value, opts.completed);
    }
    if (const nlohmann::json* value = json::find(node, "etaText")) {
        opts.eta_text = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "eta_text")) {
        opts.eta_text = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "fillStyle")) {
        opts.fill_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "fill_style")) {
        opts.fill_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "trackStyle")) {
        opts.track_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "track_style")) {
        opts.track_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "labelStyle")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "label_style")) {
        opts.label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "percentStyle")) {
        opts.percent_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "percent_style")) {
        opts.percent_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "valueStyle")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "value_style")) {
        opts.value_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "headStyle")) {
        opts.head_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "head_style")) {
        opts.head_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "insideLabelStyle")) {
        opts.inside_label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "inside_label_style")) {
        opts.inside_label_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "etaStyle")) {
        opts.eta_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "eta_style")) {
        opts.eta_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_ScrollViewOptions(tuinator::ScrollViewOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "width")) {
        opts.width = json::as_int(*value, opts.width);
    }
    if (const nlohmann::json* value = json::find(node, "height")) {
        opts.height = json::as_int(*value, opts.height);
    }
    if (const nlohmann::json* value = json::find(node, "background")) {
        opts.background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "backgroundStyle")) {
        opts.background = style::resolve_style(*value, ctx.theme);
    }
}

void fill_ScrollbarConfig(tuinator::ScrollbarConfig& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "enabled")) {
        opts.enabled = json::as_bool(*value, opts.enabled);
    }
    if (const nlohmann::json* value = json::find(node, "vertical")) {
        opts.vertical = json::as_bool(*value, opts.vertical);
    }
    if (const nlohmann::json* value = json::find(node, "horizontal")) {
        opts.horizontal = json::as_bool(*value, opts.horizontal);
    }
}

void fill_ShellTerminalStyle(tuinator::ShellTerminalStyle& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "defaultCell")) {
        opts.default_cell = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "default_cell")) {
        opts.default_cell = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "cursor")) {
        opts.cursor = style::resolve_style(*value, ctx.theme);
    }
}

void fill_SourceControlPanelStyle(tuinator::SourceControlPanelStyle& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "background")) {
        opts.background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "header")) {
        opts.header = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "sectionTitle")) {
        opts.section_title = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "section_title")) {
        opts.section_title = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "entryText")) {
        opts.entry_text = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "entry_text")) {
        opts.entry_text = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "muted")) {
        opts.muted = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "footer")) {
        opts.footer = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "panelBg")) {
        opts.panel_bg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "panel_bg")) {
        opts.panel_bg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "textFg")) {
        opts.text_fg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "text_fg")) {
        opts.text_fg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "mutedFg")) {
        opts.muted_fg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "muted_fg")) {
        opts.muted_fg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "selectionBg")) {
        opts.selection_bg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "selection_bg")) {
        opts.selection_bg = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "statusModified")) {
        opts.status_modified = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "status_modified")) {
        opts.status_modified = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "statusAdded")) {
        opts.status_added = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "status_added")) {
        opts.status_added = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "statusDeleted")) {
        opts.status_deleted = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "status_deleted")) {
        opts.status_deleted = json::as_rgb(*value);
    }
    if (const nlohmann::json* value = json::find(node, "preferredWidth")) {
        opts.preferred_width = json::as_int(*value, opts.preferred_width);
    }
    if (const nlohmann::json* value = json::find(node, "preferred_width")) {
        opts.preferred_width = json::as_int(*value, opts.preferred_width);
    }
    if (const nlohmann::json* value = json::find(node, "footerText")) {
        opts.footer_text = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "footer_text")) {
        opts.footer_text = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "showFooter")) {
        opts.show_footer = json::as_bool(*value, opts.show_footer);
    }
    if (const nlohmann::json* value = json::find(node, "show_footer")) {
        opts.show_footer = json::as_bool(*value, opts.show_footer);
    }
    if (const nlohmann::json* value = json::find(node, "nameMaxColumns")) {
        opts.name_max_columns = json::as_int(*value, opts.name_max_columns);
    }
    if (const nlohmann::json* value = json::find(node, "name_max_columns")) {
        opts.name_max_columns = json::as_int(*value, opts.name_max_columns);
    }
    if (const nlohmann::json* value = json::find(node, "tagMaxColumns")) {
        opts.tag_max_columns = json::as_int(*value, opts.tag_max_columns);
    }
    if (const nlohmann::json* value = json::find(node, "tag_max_columns")) {
        opts.tag_max_columns = json::as_int(*value, opts.tag_max_columns);
    }
}

void fill_SplitPaneOptions(tuinator::SplitPaneOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "orientation")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Horizontal") { opts.orientation = tuinator::SplitOrientation::Horizontal; }
            else if (_enum_val == "Vertical") { opts.orientation = tuinator::SplitOrientation::Vertical; }
            else { opts.orientation = tuinator::SplitOrientation::Horizontal; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "firstSize")) {
        opts.first_size = json::as_int(*value, opts.first_size);
    }
    if (const nlohmann::json* value = json::find(node, "first_size")) {
        opts.first_size = json::as_int(*value, opts.first_size);
    }
    if (const nlohmann::json* value = json::find(node, "dividerStyle")) {
        opts.divider_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "divider_style")) {
        opts.divider_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_StackedAreaChartOptions(tuinator::StackedAreaChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "minValue")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "min_value")) {
        opts.min_value = json::as_double(*value, opts.min_value);
    }
    if (const nlohmann::json* value = json::find(node, "maxValue")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "max_value")) {
        opts.max_value = json::as_double(*value, opts.max_value);
    }
    if (const nlohmann::json* value = json::find(node, "showAxes")) {
        opts.show_axes = json::as_bool(*value, opts.show_axes);
    }
    if (const nlohmann::json* value = json::find(node, "show_axes")) {
        opts.show_axes = json::as_bool(*value, opts.show_axes);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLegend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "show_legend")) {
        opts.show_legend = json::as_bool(*value, opts.show_legend);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legendStyle")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "legend_style")) {
        opts.legend_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_TabsOptions(tuinator::TabsOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "tabStyle")) {
        opts.tab_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "tab_style")) {
        opts.tab_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "selectedTabStyle")) {
        opts.selected_tab_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "selected_tab_style")) {
        opts.selected_tab_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_TerminalFrameOptions(tuinator::TerminalFrameOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "showStatusLine")) {
        opts.show_status_line = json::as_bool(*value, opts.show_status_line);
    }
    if (const nlohmann::json* value = json::find(node, "show_status_line")) {
        opts.show_status_line = json::as_bool(*value, opts.show_status_line);
    }
    if (const nlohmann::json* value = json::find(node, "liveContent")) {
        opts.live_content = json::as_bool(*value, opts.live_content);
    }
    if (const nlohmann::json* value = json::find(node, "live_content")) {
        opts.live_content = json::as_bool(*value, opts.live_content);
    }
}

void fill_TerminalFrameStyle(tuinator::TerminalFrameStyle& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "borderStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Ascii") { opts.border_style = tuinator::BorderStyle::Ascii; }
            else if (_enum_val == "Light") { opts.border_style = tuinator::BorderStyle::Light; }
            else if (_enum_val == "Heavy") { opts.border_style = tuinator::BorderStyle::Heavy; }
            else if (_enum_val == "Double") { opts.border_style = tuinator::BorderStyle::Double; }
            else if (_enum_val == "Rounded") { opts.border_style = tuinator::BorderStyle::Rounded; }
            else { opts.border_style = tuinator::BorderStyle::Ascii; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "border_style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Ascii") { opts.border_style = tuinator::BorderStyle::Ascii; }
            else if (_enum_val == "Light") { opts.border_style = tuinator::BorderStyle::Light; }
            else if (_enum_val == "Heavy") { opts.border_style = tuinator::BorderStyle::Heavy; }
            else if (_enum_val == "Double") { opts.border_style = tuinator::BorderStyle::Double; }
            else if (_enum_val == "Rounded") { opts.border_style = tuinator::BorderStyle::Rounded; }
            else { opts.border_style = tuinator::BorderStyle::Ascii; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "showControls")) {
        opts.show_controls = json::as_bool(*value, opts.show_controls);
    }
    if (const nlohmann::json* value = json::find(node, "show_controls")) {
        opts.show_controls = json::as_bool(*value, opts.show_controls);
    }
    if (const nlohmann::json* value = json::find(node, "border")) {
        opts.border = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "titleBackground")) {
        opts.title_background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_background")) {
        opts.title_background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "contentBackground")) {
        opts.content_background = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "content_background")) {
        opts.content_background = style::resolve_style(*value, ctx.theme);
    }
}

void fill_TextAreaOptions(tuinator::TextAreaOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "lineNumbers")) {
        opts.line_numbers = json::as_bool(*value, opts.line_numbers);
    }
    if (const nlohmann::json* value = json::find(node, "line_numbers")) {
        opts.line_numbers = json::as_bool(*value, opts.line_numbers);
    }
    if (const nlohmann::json* value = json::find(node, "statusBar")) {
        opts.status_bar = json::as_bool(*value, opts.status_bar);
    }
    if (const nlohmann::json* value = json::find(node, "status_bar")) {
        opts.status_bar = json::as_bool(*value, opts.status_bar);
    }
    if (const nlohmann::json* value = json::find(node, "gutterWidth")) {
        opts.gutter_width = json::as_int(*value, opts.gutter_width);
    }
    if (const nlohmann::json* value = json::find(node, "gutter_width")) {
        opts.gutter_width = json::as_int(*value, opts.gutter_width);
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "placeholder")) {
        opts.placeholder = json::as_string(*value);
    }
}

void fill_TextInputOptions(tuinator::TextInputOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "placeholder")) {
        opts.placeholder = json::as_string(*value);
    }
}

void fill_WaterfallChartOptions(tuinator::WaterfallChartOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "style")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "glyphStyle")) {
                { const std::string _enum_val = json::as_string(*value);
            if (_enum_val == "Dots") { opts.style = tuinator::ChartGlyphStyle::Dots; }
            else if (_enum_val == "FineDots") { opts.style = tuinator::ChartGlyphStyle::FineDots; }
            else if (_enum_val == "SmallDots") { opts.style = tuinator::ChartGlyphStyle::SmallDots; }
            else if (_enum_val == "Stars") { opts.style = tuinator::ChartGlyphStyle::Stars; }
            else if (_enum_val == "Hash") { opts.style = tuinator::ChartGlyphStyle::Hash; }
            else if (_enum_val == "Plus") { opts.style = tuinator::ChartGlyphStyle::Plus; }
            else if (_enum_val == "Blocks") { opts.style = tuinator::ChartGlyphStyle::Blocks; }
            else if (_enum_val == "Braille") { opts.style = tuinator::ChartGlyphStyle::Braille; }
            else if (_enum_val == "Custom") { opts.style = tuinator::ChartGlyphStyle::Custom; }
            else { opts.style = tuinator::ChartGlyphStyle::Dots; }
        }
    }
    if (const nlohmann::json* value = json::find(node, "title")) {
        opts.title = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(node, "baseline")) {
        opts.baseline = json::as_double(*value, opts.baseline);
    }
    if (const nlohmann::json* value = json::find(node, "showAxis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "show_axis")) {
        opts.show_axis = json::as_bool(*value, opts.show_axis);
    }
    if (const nlohmann::json* value = json::find(node, "showGrid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "show_grid")) {
        opts.show_grid = json::as_bool(*value, opts.show_grid);
    }
    if (const nlohmann::json* value = json::find(node, "showLabels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "show_labels")) {
        opts.show_labels = json::as_bool(*value, opts.show_labels);
    }
    if (const nlohmann::json* value = json::find(node, "showConnectors")) {
        opts.show_connectors = json::as_bool(*value, opts.show_connectors);
    }
    if (const nlohmann::json* value = json::find(node, "show_connectors")) {
        opts.show_connectors = json::as_bool(*value, opts.show_connectors);
    }
    if (const nlohmann::json* value = json::find(node, "barWidth")) {
        opts.bar_width = json::as_int(*value, opts.bar_width);
    }
    if (const nlohmann::json* value = json::find(node, "bar_width")) {
        opts.bar_width = json::as_int(*value, opts.bar_width);
    }
    if (const nlohmann::json* value = json::find(node, "barGap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "bar_gap")) {
        opts.bar_gap = json::as_int(*value, opts.bar_gap);
    }
    if (const nlohmann::json* value = json::find(node, "compactLayout")) {
        opts.compact_layout = json::as_bool(*value, opts.compact_layout);
    }
    if (const nlohmann::json* value = json::find(node, "compact_layout")) {
        opts.compact_layout = json::as_bool(*value, opts.compact_layout);
    }
    if (const nlohmann::json* value = json::find(node, "minWidth")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "min_width")) {
        opts.min_width = json::as_int(*value, opts.min_width);
    }
    if (const nlohmann::json* value = json::find(node, "minHeight")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "min_height")) {
        opts.min_height = json::as_int(*value, opts.min_height);
    }
    if (const nlohmann::json* value = json::find(node, "titleStyle")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "title_style")) {
        opts.title_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axisStyle")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "axis_style")) {
        opts.axis_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "gridStyle")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "grid_style")) {
        opts.grid_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "connectorStyle")) {
        opts.connector_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "connector_style")) {
        opts.connector_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "totalStyle")) {
        opts.total_style = style::resolve_style(*value, ctx.theme);
    }
    if (const nlohmann::json* value = json::find(node, "total_style")) {
        opts.total_style = style::resolve_style(*value, ctx.theme);
    }
}

void fill_WindowOptions(tuinator::WindowOptions& opts, const nlohmann::json& node, SceneContext& ctx) {
    if (const nlohmann::json* value = json::find(node, "modal")) {
        opts.modal = json::as_bool(*value, opts.modal);
    }
    if (const nlohmann::json* value = json::find(node, "movable")) {
        opts.movable = json::as_bool(*value, opts.movable);
    }
    if (const nlohmann::json* value = json::find(node, "resizable")) {
        opts.resizable = json::as_bool(*value, opts.resizable);
    }
    if (const nlohmann::json* value = json::find(node, "closable")) {
        opts.closable = json::as_bool(*value, opts.closable);
    }
}

tuinator::BorderGlyphs build_border_glyphs(const nlohmann::json& glyphs, SceneContext& ctx) {
    tuinator::BorderGlyphs result{};
    if (const nlohmann::json* value = json::find(glyphs, "topLeft")) {
        result.top_left = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "top_left")) {
        result.top_left = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "topRight")) {
        result.top_right = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "top_right")) {
        result.top_right = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "bottomLeft")) {
        result.bottom_left = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "bottom_left")) {
        result.bottom_left = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "bottomRight")) {
        result.bottom_right = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "bottom_right")) {
        result.bottom_right = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "horizontal")) {
        result.horizontal = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "vertical")) {
        result.vertical = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "resizeHandle")) {
        result.resize_handle = json::as_string(*value);
    }
    if (const nlohmann::json* value = json::find(glyphs, "resize_handle")) {
        result.resize_handle = json::as_string(*value);
    }
    return result;
}

const nlohmann::json& json_array_or_empty(const nlohmann::json& node, const char* key) {
    static const nlohmann::json empty = nlohmann::json::array();
    if (node.contains(key) && node[key].is_array()) {
        return node[key];
    }
    return empty;
}

tuinator::TreeNode build_tree_node(const nlohmann::json& node) {
    tuinator::TreeNode tree_node{json::node_string(node, "label", "text"), {}, json::node_bool(node, true, "expanded")};
    for (const auto& child : json_array_or_empty(node, "children")) {
        tree_node.children.push_back(build_tree_node(child));
    }
    return tree_node;
}

void build_menu_items(
    const nlohmann::json& items,
    std::vector<tuinator::MenuItem>& out,
    SceneContext& ctx) {
    for (const auto& item : items) {
        const std::string kind = json::node_string(item, "Action", "kind");
        if (kind == "Separator") {
            out.push_back(tuinator::MenuItem::separator());
            continue;
        }
        const std::string label = json::node_string(item, "label");
        const std::string shortcut = json::node_string(item, "shortcut");
        const bool enabled = json::node_bool(item, true, "enabled");
        const bool checked = json::node_bool(item, false, "checked");
        const std::string on_click = json::node_string(item, "onClick", "on_click");
        if (kind == "Checkbox") {
            out.push_back(tuinator::MenuItem::checkbox(
                label, checked, [&ctx, on_click]() {
                    if (!on_click.empty()) { ctx.invoke_handler(on_click); }
                }));
            continue;
        }
        if (kind == "Submenu") {
            std::vector<tuinator::MenuItem> children;
            const nlohmann::json& child_items = item.contains("children")
                ? item["children"]
                : json_array_or_empty(item, "items");
            build_menu_items(child_items, children, ctx);
            out.push_back(tuinator::MenuItem::submenu(
                label, std::move(children), shortcut, json::node_string(item, "icon")));
            continue;
        }
        out.push_back(tuinator::MenuItem(
            label,
            [&ctx, on_click]() {
                if (!on_click.empty()) { ctx.invoke_handler(on_click); }
            },
            shortcut,
            enabled));
    }
}

std::vector<tuinator::StatusSegment> build_status_segments(
    const nlohmann::json& segments, SceneContext& ctx) {
    std::vector<tuinator::StatusSegment> out;
    for (const auto& segment : segments) {
        tuinator::StatusSegment seg;
        const std::string kind = json::node_string(segment, "Text", "kind");
        { const std::string _enum_val = kind;
            if (_enum_val == "Text") { seg.kind = tuinator::StatusSegmentKind::Text; }
            else if (_enum_val == "Pill") { seg.kind = tuinator::StatusSegmentKind::Pill; }
            else if (_enum_val == "Box") { seg.kind = tuinator::StatusSegmentKind::Box; }
            else if (_enum_val == "Separator") { seg.kind = tuinator::StatusSegmentKind::Separator; }
            else { seg.kind = tuinator::StatusSegmentKind::Text; }
        }
        seg.text = json::node_string(segment, "text");
        seg.bold = json::node_bool(segment, false, "bold");
        seg.dim = json::node_bool(segment, false, "dim");
        if (segment.contains("foreground") && segment["foreground"].is_object()) {
            seg.foreground_rgb = json::as_rgb(segment["foreground"]);
        }
        out.push_back(seg);
    }
    return out;
}

void apply_menu_bar_look(tuinator::MenuBar& bar, const std::string& look, SceneContext& ctx) {
    const std::string preset = look;
    if (preset == "classic") {
        bar.apply_look(tuinator::menu_bar_look_classic(ctx.theme));
        return;
    }
    if (preset == "mac") {
        bar.apply_look(tuinator::menu_bar_look_mac(ctx.theme));
        return;
    }
    if (preset == "minimal") {
        bar.apply_look(tuinator::menu_bar_look_minimal(ctx.theme));
        return;
    }
    if (const tuinator::MenuBarLook* named = tuinator::menu_bar_look_named(look)) {
        bar.apply_look(*named);
    }
}

void apply_context_menu_look(tuinator::ContextMenu& menu, const std::string& look, SceneContext& ctx) {
    if (look == "classic") {
        menu.set_look(tuinator::menu_bar_look_classic(ctx.theme));
        return;
    }
    if (look == "mac") {
        menu.set_look(tuinator::menu_bar_look_mac(ctx.theme));
        return;
    }
    if (look == "minimal") {
        menu.set_look(tuinator::menu_bar_look_minimal(ctx.theme));
        return;
    }
    if (const tuinator::MenuBarLook* named = tuinator::menu_bar_look_named(look)) {
        menu.set_look(*named);
    }
}

std::vector<tuinator::BarChartBar> build_bar_chart_bars(
    const nlohmann::json& bars, SceneContext& ctx) {
    std::vector<tuinator::BarChartBar> out;
    for (const auto& bar : bars) {
        const std::string label = json::node_string(bar, "label");
        const double value = json::node_double(bar, 0.0, "value");
        if (bar.contains("style")) {
            out.push_back(tuinator::BarChartBar{
                label, value, style::resolve_style(bar["style"], ctx.theme)});
        } else {
            out.push_back(tuinator::BarChartBar{label, value});
        }
    }
    return out;
}

std::vector<tuinator::LineChartSeries> build_line_chart_series(
    const nlohmann::json& series, SceneContext& ctx) {
    std::vector<tuinator::LineChartSeries> out;
    for (const auto& entry : series) {
        const std::string label = json::node_string(entry, "label");
        std::vector<double> values;
        for (const auto& value : json_array_or_empty(entry, "values")) {
            values.push_back(json::as_double(value, 0.0));
        }
        out.push_back(tuinator::LineChartSeries{
            label,
            std::move(values),
            style::resolve_style(json::sub(entry, "style"), ctx.theme)});
    }
    return out;
}

std::vector<tuinator::StackedAreaSeries> build_stacked_area_series(
    const nlohmann::json& series, SceneContext& ctx) {
    std::vector<tuinator::StackedAreaSeries> out;
    for (const auto& entry : series) {
        const std::string label = json::node_string(entry, "label");
        std::vector<double> values;
        for (const auto& value : json_array_or_empty(entry, "values")) {
            values.push_back(json::as_double(value, 0.0));
        }
        out.push_back(tuinator::StackedAreaSeries{
            label,
            std::move(values),
            style::resolve_style(json::sub(entry, "style"), ctx.theme)});
    }
    return out;
}

tuinator::Rect parse_rect(const nlohmann::json& value) {
    if (value.is_array() && value.size() == 4) {
        return tuinator::Rect{
            value[0].get<int>(), value[1].get<int>(),
            value[2].get<int>(), value[3].get<int>()};
    }
    if (value.is_object()) {
        return tuinator::Rect{
            json::node_int(value, 0, "x"),
            json::node_int(value, 0, "y"),
            json::node_int(value, 0, "width", "w"),
            json::node_int(value, 0, "height", "h")};
    }
    return tuinator::Rect{0, 0, 40, 12};
}

void fill_scrollbar_options(
    tuinator::ScrollbarOptions& target,
    const nlohmann::json& source,
    SceneContext& ctx) {
    if (const nlohmann::json* preset = json::find(source, "preset", "scrollbarPreset", "scrollbar_preset")) {
        const std::string preset_name = json::as_string(*preset);
        tuinator::ScrollbarPreset preset_enum = tuinator::ScrollbarPreset::Classic;
        { const std::string _enum_val = preset_name;
            if (_enum_val == "Classic") { preset_enum = tuinator::ScrollbarPreset::Classic; }
            else if (_enum_val == "Ascii") { preset_enum = tuinator::ScrollbarPreset::Ascii; }
            else if (_enum_val == "Minimal") { preset_enum = tuinator::ScrollbarPreset::Minimal; }
            else if (_enum_val == "Bold") { preset_enum = tuinator::ScrollbarPreset::Bold; }
            else if (_enum_val == "Thin") { preset_enum = tuinator::ScrollbarPreset::Thin; }
            else { preset_enum = tuinator::ScrollbarPreset::Classic; }
        }
        target = tuinator::scrollbar_options(ctx.theme, preset_enum);
    }
    if (const nlohmann::json* config = json::find(source, "config")) {
        fill_ScrollbarConfig(target.config, *config, ctx);
    } else {
        fill_ScrollbarConfig(target.config, source, ctx);
    }
}

void create_windows(
    tuinator::Desktop& host,
    const char* method,
    const nlohmann::json& windows,
    SceneContext& ctx,
    bool allow_modal) {
    for (const auto& win : windows) {
        if (!win.contains("content")) {
            continue;
        }
        auto content = build_widget(win["content"], ctx);
        const tuinator::Rect bounds = parse_rect(json::sub(win, "bounds"));
        const std::string title = json::node_string_default(win, "Window", "title");
        tuinator::WindowOptions win_opts{};
        fill_WindowOptions(win_opts, win, ctx);
        const bool modal = json::node_bool(win, false, "modal");
        if (modal && allow_modal) {
            host.show_modal(title, bounds, std::move(content));
            continue;
        }
        tuinator::Window* window_ptr = nullptr;
        if (std::string(method) == "create_window") {
            window_ptr = host.create_window(title, bounds, std::move(content), win_opts);
        }
        const std::string on_close = json::node_string(win, "onClose", "on_close");
        if (window_ptr && !on_close.empty()) {
            ctx.schedule_post_init([window_ptr, &ctx, on_close]() {
                window_ptr->set_on_close([&ctx, on_close]() { ctx.invoke_handler(on_close); });
            });
        }
    }
}

void create_windows(
    tuinator::WindowHost& host,
    const char* method,
    const nlohmann::json& windows,
    SceneContext& ctx,
    bool allow_modal) {
    for (const auto& win : windows) {
        if (!win.contains("content")) {
            continue;
        }
        auto content = build_widget(win["content"], ctx);
        const tuinator::Rect bounds = parse_rect(json::sub(win, "bounds"));
        const std::string title = json::node_string_default(win, "Window", "title");
        tuinator::WindowOptions win_opts{};
        fill_WindowOptions(win_opts, win, ctx);
        const bool modal = json::node_bool(win, false, "modal");
        if (modal && allow_modal) {
            throw std::runtime_error("Modal windows require a Desktop host");
        }
        tuinator::Window* window_ptr = nullptr;
        if (std::string(method) == "create_window") {
            window_ptr = host.create_window(title, bounds, std::move(content), win_opts);
        }
        const std::string on_close = json::node_string(win, "onClose", "on_close");
        if (window_ptr && !on_close.empty()) {
            ctx.schedule_post_init([window_ptr, &ctx, on_close]() {
                window_ptr->set_on_close([&ctx, on_close]() { ctx.invoke_handler(on_close); });
            });
        }
    }
}

#if defined(_MSC_VER)
#pragma warning(pop)
#endif

#if defined(__GNUC__) || defined(__clang__)
#pragma GCC diagnostic pop
#endif

} // namespace

std::unique_ptr<Widget> build_widget(const nlohmann::json& node, SceneContext& ctx) {
    const std::string type = json::node_string(node, "type");

    if (type == "VBox") {
        auto widget = std::make_unique<tuinator::VBox>(tuinator::BoxOptions{json::node_int(node, 1, "gap"), json::node_int(node, 0, "padding")});
        if (const nlohmann::json* flex = json::find(node, "flex")) {
            widget->set_flex(json::as_int(*flex, 0));
        }
        tuinator::VBox* raw = widget.get();
        for (const auto& child_node : json_array_or_empty(node, "children")) {
            auto child = build_widget(child_node, ctx);
            widget->add_child(std::move(child));
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "VBox", raw);
        }
        return widget;
    }
    if (type == "HBox") {
        auto widget = std::make_unique<tuinator::HBox>(tuinator::BoxOptions{json::node_int(node, 1, "gap"), json::node_int(node, 0, "padding")});
        if (const nlohmann::json* flex = json::find(node, "flex")) {
            widget->set_flex(json::as_int(*flex, 0));
        }
        tuinator::HBox* raw = widget.get();
        for (const auto& child_node : json_array_or_empty(node, "children")) {
            auto child = build_widget(child_node, ctx);
            widget->add_child(std::move(child));
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "HBox", raw);
        }
        return widget;
    }
    if (type == "Grid") {
        auto widget = std::make_unique<tuinator::Grid>(tuinator::GridOptions{json::node_int(node, 2, "columns"), json::node_int(node, 1, "gap"), json::node_int(node, 0, "padding")});
        tuinator::Grid* raw = widget.get();
        for (const auto& child_node : json_array_or_empty(node, "children")) {
            auto child = build_widget(child_node, ctx);
            widget->add_child(std::move(child));
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Grid", raw);
        }
        return widget;
    }
    if (type == "Panel") {
        const std::string title = json::node_string(node, "title");
        const tuinator::Style border_style = style::resolve_style(json::sub(node, "borderStyle", "border"), ctx.theme);
        const tuinator::Style title_style = style::resolve_style(json::sub(node, "titleStyle", "heading"), ctx.theme);
        if (node.contains("children")) {
            throw std::runtime_error("Panel supports a single 'content' node, not 'children'");
        }
        std::unique_ptr<tuinator::Panel> widget;
        if (const nlohmann::json* glyphs = json::find(node, "borderGlyphs", "border_glyphs", "glyphs")) {
            widget = std::make_unique<tuinator::Panel>(
                title, border_style, title_style, build_border_glyphs(*glyphs, ctx));
        } else {
            widget = std::make_unique<tuinator::Panel>(title, border_style, title_style);
        }
        if (node.contains("content")) {
            auto content = build_widget(node["content"], ctx);
            widget->set_content(std::move(content));
        }
        tuinator::Panel* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Panel", raw);
        }
        return widget;
    }
    if (type == "ScrollView") {
        if (!node.contains("content")) {
            throw std::runtime_error("ScrollView requires a 'content' node");
        }
        auto content = build_widget(node["content"], ctx);
        tuinator::ScrollViewOptions opts{};
        fill_ScrollViewOptions(opts, node, ctx);
        if (const nlohmann::json* scrollbars = json::find(node, "scrollbars", "scrollbar")) {
            fill_scrollbar_options(opts.scrollbars, *scrollbars, ctx);
        }
        auto widget = std::make_unique<tuinator::ScrollView>(std::move(content), opts);
        tuinator::ScrollView* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ScrollView", raw);
        }
        return widget;
    }
    if (type == "SplitPane") {
        if (!node.contains("first") || !node.contains("second")) {
            throw std::runtime_error("SplitPane requires 'first' and 'second' nodes");
        }
        auto first = build_widget(node["first"], ctx);
        auto second = build_widget(node["second"], ctx);
        tuinator::SplitPaneOptions opts{};
        fill_SplitPaneOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::SplitPane>(
            std::move(first), std::move(second), opts);
        tuinator::SplitPane* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "SplitPane", raw);
        }
        return widget;
    }
    if (type == "Tabs") {
        tuinator::TabsOptions opts{};
        fill_TabsOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::Tabs>(opts);
        tuinator::Tabs* raw = widget.get();
        for (const auto& tab : json_array_or_empty(node, "tabs")) {
            const std::string title = json::node_string(tab, "Tab", "title");
            if (!tab.contains("content")) {
                throw std::runtime_error("Each tab requires a 'content' node");
            }
            auto content = build_widget(tab["content"], ctx);
            widget->add_tab(title, std::move(content));
        }
        if (const nlohmann::json* selected = json::find(node, "selectedIndex", "selected_index")) {
            ctx.schedule_post_init([raw, selected_index = json::as_int(*selected, 0)]() {
                raw->set_selected_index(selected_index);
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Tabs", raw);
        }
        return widget;
    }
    if (type == "TerminalFrame") {
        const std::string title = json::node_string_default(node, "Terminal", "title");
        if (!node.contains("content")) {
            throw std::runtime_error("TerminalFrame requires a 'content' node");
        }
        auto content = build_widget(node["content"], ctx);
        tuinator::TerminalFrameOptions opts{};
        fill_TerminalFrameOptions(opts, node, ctx);
        if (const nlohmann::json* frame_style = json::find(node, "frameStyle", "frame_style", "style")) {
            fill_TerminalFrameStyle(opts.style, *frame_style, ctx);
        }
        auto widget = std::make_unique<tuinator::TerminalFrame>(title, std::move(content), opts);
        tuinator::TerminalFrame* raw = widget.get();
        if (const nlohmann::json* left = json::find(node, "statusLeft", "status_left")) {
            auto segs = build_status_segments(*left, ctx);
            ctx.schedule_post_init([raw, segs = std::move(segs)]() mutable {
                raw->set_status_left(std::move(segs));
            });
        }
        if (const nlohmann::json* center = json::find(node, "statusCenter", "status_center")) {
            auto segs = build_status_segments(*center, ctx);
            ctx.schedule_post_init([raw, segs = std::move(segs)]() mutable {
                raw->set_status_center(std::move(segs));
            });
        }
        if (const nlohmann::json* right = json::find(node, "statusRight", "status_right")) {
            auto segs = build_status_segments(*right, ctx);
            ctx.schedule_post_init([raw, segs = std::move(segs)]() mutable {
                raw->set_status_right(std::move(segs));
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "TerminalFrame", raw);
        }
        return widget;
    }
    if (type == "Label") {
        auto widget = std::make_unique<tuinator::Label>(json::node_string(node, "text"), style::resolve_style(json::sub(node, "style"), ctx.theme));
        tuinator::Label* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Label", raw);
        }
        return widget;
    }
    if (type == "Button") {
        const std::string label = json::node_string_default(node, "Button", "label", "text");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const std::string on_click = json::node_string(node, "onClick", "on_click");
        auto widget = std::make_unique<tuinator::Button>(
            label, [&ctx, on_click]() { if (!on_click.empty()) { ctx.invoke_handler(on_click); } }, style);
        tuinator::Button* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Button", raw);
        }
        return widget;
    }
    if (type == "TextInput") {
        tuinator::TextInputOptions opts{};
        fill_TextInputOptions(opts, node, ctx);
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const tuinator::Style focused = style::resolve_style(json::sub(node, "focusedStyle", "text_input_focused"), ctx.theme);
        auto widget = std::make_unique<tuinator::TextInput>(opts, style, focused);
        if (const nlohmann::json* value = json::find(node, "value")) {
            widget->set_value(json::as_string(*value));
        }
        tuinator::TextInput* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "TextInput", raw);
        }
        return widget;
    }
    if (type == "TextArea") {
        tuinator::TextAreaOptions opts{};
        fill_TextAreaOptions(opts, node, ctx);
        if (const nlohmann::json* scrollbars = json::find(node, "scrollbars", "scrollbar")) {
            fill_scrollbar_options(opts.scrollbars, *scrollbars, ctx);
        }
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const tuinator::Style focused = style::resolve_style(json::sub(node, "focusedStyle", "text_input_focused"), ctx.theme);
        auto widget = std::make_unique<tuinator::TextArea>(opts, style, focused);
        if (const nlohmann::json* value = json::find(node, "value")) {
            widget->set_value(json::as_string(*value));
        }
        tuinator::TextArea* raw = widget.get();
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        if (!on_change.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_change]() {
                raw->set_on_change([&ctx, on_change](const std::string& /*value*/) {
                    ctx.invoke_handler(on_change);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "TextArea", raw);
        }
        return widget;
    }
    if (type == "Separator") {
        auto widget = std::make_unique<tuinator::Separator>(style::resolve_style(json::sub(node, "style", "border"), ctx.theme));
        tuinator::Separator* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Separator", raw);
        }
        return widget;
    }
    if (type == "Checkbox") {
        const std::string label = json::node_string(node, "label", "text");
        const bool checked = json::node_bool(node, false, "checked");
        tuinator::CheckboxOptions opts = tuinator::checkbox_options_default(ctx.theme);
        const std::string style_name = json::node_string(node, "styleName", "style_name", "checkboxStyle", "checkbox_style");
        if (!style_name.empty()) {
            if (const tuinator::CheckboxStyle* named = tuinator::checkbox_style_named(style_name)) {
                tuinator::apply_checkbox_style(opts, *named, ctx.theme);
            }
        }
        fill_CheckboxOptions(opts, node, ctx);
        if (const nlohmann::json* glyphs = json::find(node, "glyphs")) {
            fill_CheckboxGlyphs(opts.glyphs, *glyphs, ctx);
        }
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        auto widget = std::make_unique<tuinator::Checkbox>(
            label, checked, opts, [&ctx, on_change](bool /*value*/) {
                if (!on_change.empty()) { ctx.invoke_handler(on_change); }
            });
        tuinator::Checkbox* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Checkbox", raw);
        }
        return widget;
    }
    if (type == "Toggle") {
        const std::string label = json::node_string(node, "label", "text");
        const bool checked = json::node_bool(node, false, "checked");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const tuinator::Style checked_style = style::resolve_style(json::sub(node, "checkedStyle", "accent"), ctx.theme);
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        auto widget = std::make_unique<tuinator::Toggle>(
            label, checked, [&ctx, on_change](bool /*value*/) {
                if (!on_change.empty()) { ctx.invoke_handler(on_change); }
            }, style, checked_style);
        tuinator::Toggle* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Toggle", raw);
        }
        return widget;
    }
    if (type == "Slider") {
        const int min_value = json::node_int(node, 0, "min", "minValue");
        const int max_value = json::node_int(node, 100, "max", "maxValue");
        const int value = json::node_int(node, 0, "value");
        const int min_width = json::node_int(node, 20, "minWidth", "min_width");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        auto widget = std::make_unique<tuinator::Slider>(
            min_value, max_value, value, [&ctx, on_change](int /*value*/) {
                if (!on_change.empty()) { ctx.invoke_handler(on_change); }
            }, style, min_width);
        tuinator::Slider* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Slider", raw);
        }
        return widget;
    }
    if (type == "Spinner") {
        const int min_value = json::node_int(node, 0, "min", "minValue");
        const int max_value = json::node_int(node, 100, "max", "maxValue");
        const int value = json::node_int(node, 0, "value");
        const int step = json::node_int(node, 1, "step");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        auto widget = std::make_unique<tuinator::Spinner>(
            min_value, max_value, value, step, [&ctx, on_change](int /*value*/) {
                if (!on_change.empty()) { ctx.invoke_handler(on_change); }
            }, style);
        tuinator::Spinner* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Spinner", raw);
        }
        return widget;
    }
    if (type == "ComboBox") {
        const tuinator::Style item_style = style::resolve_style(json::sub(node, "itemStyle"), ctx.theme);
        const tuinator::Style selected_style = style::resolve_style(json::sub(node, "selectedStyle", "accent"), ctx.theme);
        auto widget = std::make_unique<tuinator::ComboBox>(item_style, selected_style);
        tuinator::ComboBox* raw = widget.get();
        if (const nlohmann::json* items = json::find(node, "items")) {
            std::vector<std::string> item_values;
            for (const auto& item : *items) { item_values.push_back(json::as_string(item)); }
            widget->set_items(std::move(item_values));
        }
        if (const nlohmann::json* selected = json::find(node, "selectedIndex", "selected_index")) {
            widget->set_selected_index(json::as_int(*selected, 0));
        }
        const std::string on_select = json::node_string(node, "onSelect", "on_select");
        if (!on_select.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_select]() {
                raw->set_on_select([&ctx, on_select](int /*index*/, const std::string& /*item*/) {
                    ctx.invoke_handler(on_select);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ComboBox", raw);
        }
        return widget;
    }
    if (type == "ProgressBar") {
        const double value = json::node_double(node, 0.0, "value");
        tuinator::ProgressBarOptions opts{};
        const std::string layout = json::node_string(node, "layout");
        if (!layout.empty()) {
            { const std::string _enum_val = layout;
                if (_enum_val == "Plain") { opts.layout = tuinator::ProgressBarLayout::Plain; }
                else if (_enum_val == "Bracketed") { opts.layout = tuinator::ProgressBarLayout::Bracketed; }
                else if (_enum_val == "Blocks") { opts.layout = tuinator::ProgressBarLayout::Blocks; }
                else if (_enum_val == "Dots") { opts.layout = tuinator::ProgressBarLayout::Dots; }
                else if (_enum_val == "Pill") { opts.layout = tuinator::ProgressBarLayout::Pill; }
                else if (_enum_val == "Labeled") { opts.layout = tuinator::ProgressBarLayout::Labeled; }
                else if (_enum_val == "FilledLabel") { opts.layout = tuinator::ProgressBarLayout::FilledLabel; }
                else if (_enum_val == "Tqdm") { opts.layout = tuinator::ProgressBarLayout::Tqdm; }
                else if (_enum_val == "BrailleMetric") { opts.layout = tuinator::ProgressBarLayout::BrailleMetric; }
                else if (_enum_val == "BrailleWave") { opts.layout = tuinator::ProgressBarLayout::BrailleWave; }
                else if (_enum_val == "Pulse") { opts.layout = tuinator::ProgressBarLayout::Pulse; }
                else if (_enum_val == "Shimmer") { opts.layout = tuinator::ProgressBarLayout::Shimmer; }
                else if (_enum_val == "Bounce") { opts.layout = tuinator::ProgressBarLayout::Bounce; }
                else if (_enum_val == "SlideBlock") { opts.layout = tuinator::ProgressBarLayout::SlideBlock; }
                else if (_enum_val == "MovingDot") { opts.layout = tuinator::ProgressBarLayout::MovingDot; }
                else if (_enum_val == "TaskRow") { opts.layout = tuinator::ProgressBarLayout::TaskRow; }
                else { opts.layout = tuinator::ProgressBarLayout::Plain; }
            }
            opts = tuinator::progress_bar_preset(opts.layout, ctx.theme.accent, ctx.theme.muted);
        }
        fill_ProgressBarOptions(opts, node, ctx);
        if (const nlohmann::json* stats = json::find(node, "stats")) {
            opts.stats.current = json::node_int(*stats, 0, "current");
            opts.stats.total = json::node_int(*stats, 100, "total");
            opts.stats.unit = json::node_string(*stats, "step", "unit");
            opts.stats.rate = json::node_double(*stats, 0.0, "rate");
            opts.stats.elapsed_ms = json::node_int(*stats, 0, "elapsedMs", "elapsed_ms");
        }
        if (const nlohmann::json* glyphs = json::find(node, "glyphs")) {
            fill_ProgressBarGlyphs(opts.glyphs, *glyphs, ctx);
        }
        if (const nlohmann::json* stops = json::find(node, "gradientStops", "gradient_stops")) {
            opts.gradient_stops = json::build_progress_bar_gradient(*stops, ctx.theme);
        }
        auto widget = std::make_unique<tuinator::ProgressBar>(value, opts);
        tuinator::ProgressBar* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ProgressBar", raw);
        }
        return widget;
    }
    if (type == "Throbber") {
        const std::string name = json::node_string_default(node, "dots", "name", "set");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        auto widget = std::make_unique<tuinator::Throbber>(name, style);
        tuinator::Throbber* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Throbber", raw);
        }
        return widget;
    }
    if (type == "BigText") {
        const std::string text = json::node_string(node, "text");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const std::string look_name = json::node_string(node, "lookName", "look_name", "look");
        const std::string style_name = json::node_string(node, "styleName", "style_name");
        const std::string kind = json::node_string(node, "kind");
        auto widget = [&]() -> std::unique_ptr<tuinator::BigText> {
            tuinator::BigTextOptions opts{};
            if (!look_name.empty()) {
                if (const tuinator::BigTextLook* look = tuinator::big_text_look_named(look_name)) {
                    tuinator::apply_big_text_look(opts, *look);
                }
            }
            fill_BigTextOptions(opts, node, ctx);
            if (const nlohmann::json* stops = json::find(node, "gradientStops", "gradient_stops")) {
                opts.gradient_stops = json::build_big_text_gradient(*stops, ctx.theme);
            }
            if (const nlohmann::json* palette = json::find(node, "palette")) {
                for (const auto& entry : *palette) {
                    opts.palette.push_back(style::resolve_style(entry, ctx.theme));
                }
            }
            if (!look_name.empty() || json::has_any_option(node, {
                "kind", "letterSpacing", "letter_spacing", "scale", "rainbow", "underline",
                "gradientAxis", "gradient_axis", "gradientStops", "gradient_stops",
                "shadowStyle", "shadow_style", "shadowDx", "shadow_dx", "shadowDy", "shadow_dy",
                "shadowLayers", "shadow_layers", "palette", "look", "lookName", "look_name"})) {
                return std::make_unique<tuinator::BigText>(text, opts, style);
            }
            if (!style_name.empty()) {
                return std::make_unique<tuinator::BigText>(text, style_name, style);
            }
            if (!kind.empty()) {
                { const std::string _enum_val = kind;
                    if (_enum_val == "Block") { opts.kind = tuinator::BigTextKind::Block; }
                    else if (_enum_val == "Half") { opts.kind = tuinator::BigTextKind::Half; }
                    else if (_enum_val == "Narrow") { opts.kind = tuinator::BigTextKind::Narrow; }
                    else if (_enum_val == "Quadrant") { opts.kind = tuinator::BigTextKind::Quadrant; }
                    else if (_enum_val == "Third") { opts.kind = tuinator::BigTextKind::Third; }
                    else if (_enum_val == "Sextant") { opts.kind = tuinator::BigTextKind::Sextant; }
                    else if (_enum_val == "Quarter") { opts.kind = tuinator::BigTextKind::Quarter; }
                    else if (_enum_val == "Octant") { opts.kind = tuinator::BigTextKind::Octant; }
                    else if (_enum_val == "Braille") { opts.kind = tuinator::BigTextKind::Braille; }
                    else if (_enum_val == "Shade") { opts.kind = tuinator::BigTextKind::Shade; }
                    else if (_enum_val == "Letter") { opts.kind = tuinator::BigTextKind::Letter; }
                    else if (_enum_val == "Banner") { opts.kind = tuinator::BigTextKind::Banner; }
                    else if (_enum_val == "Outline") { opts.kind = tuinator::BigTextKind::Outline; }
                    else if (_enum_val == "Stacked") { opts.kind = tuinator::BigTextKind::Stacked; }
                    else if (_enum_val == "Isometric") { opts.kind = tuinator::BigTextKind::Isometric; }
                    else if (_enum_val == "Slant") { opts.kind = tuinator::BigTextKind::Slant; }
                    else if (_enum_val == "Standard") { opts.kind = tuinator::BigTextKind::Standard; }
                    else if (_enum_val == "Small") { opts.kind = tuinator::BigTextKind::Small; }
                    else if (_enum_val == "Big") { opts.kind = tuinator::BigTextKind::Big; }
                    else if (_enum_val == "Doom") { opts.kind = tuinator::BigTextKind::Doom; }
                    else if (_enum_val == "FigletShadow") { opts.kind = tuinator::BigTextKind::FigletShadow; }
                    else { opts.kind = tuinator::BigTextKind::Block; }
                }
                return std::make_unique<tuinator::BigText>(text, opts.kind, style);
            }
            return std::make_unique<tuinator::BigText>(text, style);
        }();
        tuinator::BigText* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "BigText", raw);
        }
        return widget;
    }
    if (type == "ImageView") {
        const std::string path = json::node_string_default(node, "examples/assets/lenna.png", "path", "image");
        const int width = json::node_int(node, 0, "width", "displayWidth");
        const int height = json::node_int(node, 0, "height", "displayHeight");
        tuinator::TerminalImage image;
        if (auto loaded = tuinator::TerminalImage::load_png(path)) {
            image = std::move(*loaded);
        } else if (auto loaded = tuinator::TerminalImage::load_ppm(path)) {
            image = std::move(*loaded);
        }
        std::unique_ptr<tuinator::ImageView> widget;
        if (width > 0 && height > 0) {
            widget = std::make_unique<tuinator::ImageView>(
                std::move(image), tuinator::Size{width, height});
        } else {
            widget = std::make_unique<tuinator::ImageView>(std::move(image));
        }
        tuinator::ImageView* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ImageView", raw);
        }
        return widget;
    }
    if (type == "NerdIconCatalogGallery") {
        tuinator::NerdIconCatalogGalleryStyle style_opts{};
        fill_NerdIconCatalogGalleryStyle(style_opts, node, ctx);
        auto widget = std::make_unique<tuinator::NerdIconCatalogGallery>(style_opts);
        tuinator::NerdIconCatalogGallery* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "NerdIconCatalogGallery", raw);
        }
        return widget;
    }
    if (type == "NerdGlyphMatrixGallery") {
        const int columns = json::node_int(node, 32, "columns");
        const std::string data_root = json::node_string_default(node, "data", "dataRoot", "data_root");
        auto sections = tuinator::load_nerd_glyph_catalog(data_root);
        tuinator::NerdGlyphMatrixGalleryStyle style_opts{};
        fill_NerdGlyphMatrixGalleryStyle(style_opts, node, ctx);
        auto widget = std::make_unique<tuinator::NerdGlyphMatrixGallery>(
            std::move(sections), columns, style_opts);
        tuinator::NerdGlyphMatrixGallery* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "NerdGlyphMatrixGallery", raw);
        }
        return widget;
    }
    if (type == "ListView") {
        const tuinator::Style item_style = style::resolve_style(json::sub(node, "itemStyle"), ctx.theme);
        const tuinator::Style selected_style = style::resolve_style(json::sub(node, "selectedStyle", "accent"), ctx.theme);
        auto widget = std::make_unique<tuinator::ListView>(item_style, selected_style);
        tuinator::ListView* raw = widget.get();
        if (const nlohmann::json* items = json::find(node, "items")) {
            std::vector<std::string> item_values;
            for (const auto& item : *items) { item_values.push_back(json::as_string(item)); }
            widget->set_items(std::move(item_values));
        }
        if (const nlohmann::json* selected = json::find(node, "selectedIndex", "selected_index")) {
            widget->set_selected_index(json::as_int(*selected, 0));
        }
        const std::string on_select = json::node_string(node, "onSelect", "on_select");
        const std::string on_activate = json::node_string(node, "onActivate", "on_activate");
        if (!on_select.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_select]() {
                raw->set_on_select([&ctx, on_select](int /*index*/, const std::string& /*item*/) {
                    ctx.invoke_handler(on_select);
                });
            });
        }
        if (!on_activate.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_activate]() {
                raw->set_on_activate([&ctx, on_activate](int /*index*/, const std::string& /*item*/) {
                    ctx.invoke_handler(on_activate);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ListView", raw);
        }
        return widget;
    }
    if (type == "Table") {
        const tuinator::Style header_style = style::resolve_style(json::sub(node, "headerStyle", "heading"), ctx.theme);
        const tuinator::Style cell_style = style::resolve_style(json::sub(node, "cellStyle"), ctx.theme);
        const tuinator::Style selected_style = style::resolve_style(json::sub(node, "selectedStyle", "accent"), ctx.theme);
        auto widget = std::make_unique<tuinator::Table>(header_style, cell_style, selected_style);
        tuinator::Table* raw = widget.get();
        if (const nlohmann::json* columns = json::find(node, "columns")) {
            std::vector<tuinator::TableColumn> cols;
            for (const auto& col : *columns) {
                const std::string title = col.is_object() ? json::node_string(col, "title") : json::as_string(col);
                const int width = col.is_object() ? json::node_int(col, 10, "width") : 10;
                cols.push_back(tuinator::TableColumn{title, width});
            }
            widget->set_columns(std::move(cols));
        }
        if (const nlohmann::json* rows = json::find(node, "rows")) {
            std::vector<std::vector<std::string>> table_rows;
            for (const auto& row : *rows) {
                std::vector<std::string> cells;
                for (const auto& cell : row) { cells.push_back(json::as_string(cell)); }
                table_rows.push_back(std::move(cells));
            }
            widget->set_rows(std::move(table_rows));
        }
        const std::string on_activate = json::node_string(node, "onActivate", "on_activate");
        if (!on_activate.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_activate]() {
                raw->set_on_activate([&ctx, on_activate](int /*row*/, const std::vector<std::string>& /*cells*/) {
                    ctx.invoke_handler(on_activate);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Table", raw);
        }
        return widget;
    }
    if (type == "TreeView") {
        const tuinator::Style item_style = style::resolve_style(json::sub(node, "itemStyle"), ctx.theme);
        const tuinator::Style selected_style = style::resolve_style(json::sub(node, "selectedStyle", "accent"), ctx.theme);
        auto widget = std::make_unique<tuinator::TreeView>(item_style, selected_style);
        tuinator::TreeView* raw = widget.get();
        if (const nlohmann::json* root = json::find(node, "root")) {
            widget->set_root(build_tree_node(*root));
        }
        const std::string on_select = json::node_string(node, "onSelect", "on_select");
        if (!on_select.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_select]() {
                raw->set_on_select([&ctx, on_select](const std::string& /*path*/) {
                    ctx.invoke_handler(on_select);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "TreeView", raw);
        }
        return widget;
    }
    if (type == "BarChart") {
        const nlohmann::json& bars_json = node.contains("bars") ? node["bars"] : nlohmann::json::array();
        auto bars = build_bar_chart_bars(bars_json, ctx);
        tuinator::BarChartOptions opts{};
        fill_BarChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::BarChart>(std::move(bars), opts);
        tuinator::BarChart* raw = widget.get();
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        if (!on_change.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_change]() {
                raw->set_on_change([&ctx, on_change](const std::vector<tuinator::BarChartBar>& /*bars*/) {
                    ctx.invoke_handler(on_change);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "BarChart", raw);
        }
        return widget;
    }
    if (type == "LineChart") {
        const nlohmann::json& series_json = node.contains("series") ? node["series"] : nlohmann::json::array();
        auto series = build_line_chart_series(series_json, ctx);
        tuinator::LineChartOptions opts{};
        fill_LineChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::LineChart>(std::move(series), opts);
        tuinator::LineChart* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "LineChart", raw);
        }
        return widget;
    }
    if (type == "PieChart") {
        std::vector<tuinator::PieChartSlice> slices;
        for (const auto& slice_node : json_array_or_empty(node, "slices")) {
            const std::string label = json::node_string(slice_node, "label");
            const double value = json::node_double(slice_node, 0.0, "value");
            if (slice_node.contains("style")) {
                slices.push_back(tuinator::PieChartSlice{
                    label, value, style::resolve_style(slice_node["style"], ctx.theme)});
            } else {
                slices.push_back(tuinator::PieChartSlice{label, value});
            }
        }
        tuinator::PieChartOptions opts{};
        fill_PieChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::PieChart>(std::move(slices), opts);
        tuinator::PieChart* raw = widget.get();
        const std::string on_change = json::node_string(node, "onChange", "on_change");
        if (!on_change.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_change]() {
                raw->set_on_change([&ctx, on_change](const std::vector<tuinator::PieChartSlice>& /*slices*/) {
                    ctx.invoke_handler(on_change);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "PieChart", raw);
        }
        return widget;
    }
    if (type == "GaugeChart") {
        const double value = json::node_double(node, 0.0, "value");
        tuinator::GaugeChartOptions opts{};
        fill_GaugeChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::GaugeChart>(value, opts);
        tuinator::GaugeChart* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "GaugeChart", raw);
        }
        return widget;
    }
    if (type == "Histogram") {
        std::vector<tuinator::HistogramBin> bins;
        for (const auto& bin_node : json_array_or_empty(node, "bins")) {
            const std::string label = json::node_string(bin_node, "label");
            const double count = json::node_double(bin_node, 0.0, "count");
            if (bin_node.contains("style")) {
                bins.push_back(tuinator::HistogramBin{
                    label, count, style::resolve_style(bin_node["style"], ctx.theme)});
            } else {
                bins.push_back(tuinator::HistogramBin{label, count});
            }
        }
        tuinator::HistogramOptions opts{};
        fill_HistogramOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::Histogram>(std::move(bins), opts);
        tuinator::Histogram* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Histogram", raw);
        }
        return widget;
    }
    if (type == "Heatmap") {
        std::vector<std::vector<double>> values;
        for (const auto& row : json_array_or_empty(node, "values")) {
            std::vector<double> row_values;
            for (const auto& cell : row) { row_values.push_back(json::as_double(cell, 0.0)); }
            values.push_back(std::move(row_values));
        }
        std::vector<std::string> row_labels;
        if (const nlohmann::json* rows = json::find(node, "rowLabels", "row_labels")) {
            for (const auto& label : *rows) { row_labels.push_back(json::as_string(label)); }
        }
        std::vector<std::string> col_labels;
        if (const nlohmann::json* cols = json::find(node, "colLabels", "col_labels")) {
            for (const auto& label : *cols) { col_labels.push_back(json::as_string(label)); }
        }
        tuinator::HeatmapOptions opts{};
        fill_HeatmapOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::Heatmap>(
            std::move(values), std::move(row_labels), std::move(col_labels), opts);
        tuinator::Heatmap* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Heatmap", raw);
        }
        return widget;
    }
    if (type == "CandlestickChart") {
        std::vector<tuinator::OhlcBar> bars;
        for (const auto& bar_node : json_array_or_empty(node, "bars")) {
            if (!bar_node.is_object()) { continue; }
            bars.push_back(tuinator::OhlcBar{
                json::node_string(bar_node, "label"),
                json::node_double(bar_node, 0.0, "open"),
                json::node_double(bar_node, 0.0, "high"),
                json::node_double(bar_node, 0.0, "low"),
                json::node_double(bar_node, 0.0, "close"),
                json::node_double(bar_node, 0.0, "volume")});
        }
        tuinator::CandlestickChartOptions opts{};
        fill_CandlestickChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::CandlestickChart>(std::move(bars), opts);
        tuinator::CandlestickChart* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "CandlestickChart", raw);
        }
        return widget;
    }
    if (type == "StackedAreaChart") {
        const nlohmann::json& series_json = node.contains("series") ? node["series"] : nlohmann::json::array();
        auto series = build_stacked_area_series(series_json, ctx);
        tuinator::StackedAreaChartOptions opts{};
        fill_StackedAreaChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::StackedAreaChart>(std::move(series), opts);
        tuinator::StackedAreaChart* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "StackedAreaChart", raw);
        }
        return widget;
    }
    if (type == "WaterfallChart") {
        std::vector<tuinator::WaterfallStep> steps;
        for (const auto& step_node : json_array_or_empty(node, "steps")) {
            if (!step_node.is_object()) { continue; }
            const std::string label = json::node_string(step_node, "label");
            const double delta = json::node_double(step_node, 0.0, "delta");
            const nlohmann::json* up = json::find(step_node, "upStyle", "up_style");
            const nlohmann::json* down = json::find(step_node, "downStyle", "down_style");
            if (up || down) {
                steps.push_back(tuinator::WaterfallStep{
                    label, delta,
                    style::resolve_style(up ? *up : nlohmann::json{}, ctx.theme),
                    style::resolve_style(down ? *down : nlohmann::json{}, ctx.theme)});
            } else {
                steps.push_back(tuinator::WaterfallStep{label, delta});
            }
        }
        tuinator::WaterfallChartOptions opts{};
        fill_WaterfallChartOptions(opts, node, ctx);
        auto widget = std::make_unique<tuinator::WaterfallChart>(std::move(steps), opts);
        tuinator::WaterfallChart* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "WaterfallChart", raw);
        }
        return widget;
    }
    if (type == "MenuBar") {
        const tuinator::Style style = style::resolve_style(json::sub(node, "style"), ctx.theme);
        const tuinator::Style active_style = style::resolve_style(json::sub(node, "activeStyle", "accent"), ctx.theme);
        auto widget = std::make_unique<tuinator::MenuBar>(style, active_style);
        tuinator::MenuBar* raw = widget.get();
        if (const nlohmann::json* menus = json::find(node, "menus")) {
            std::vector<tuinator::Menu> menu_values;
            for (const auto& menu : *menus) {
                const std::string title = json::node_string(menu, "title");
                std::vector<tuinator::MenuItem> items;
                build_menu_items(json_array_or_empty(menu, "items"), items, ctx);
                menu_values.push_back(tuinator::Menu{title, std::move(items)});
            }
            widget->set_menus(std::move(menu_values));
        }
        const std::string look = json::node_string(node, "look", "lookName", "look_name");
        if (!look.empty()) {
            ctx.schedule_post_init([raw, &ctx, look]() { apply_menu_bar_look(*raw, look, ctx); });
        }
        const std::string on_action = json::node_string(node, "onAction", "on_action");
        if (!on_action.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_action]() {
                raw->set_on_action([&ctx, on_action](const std::string& /*menu*/, const std::string& /*item*/) {
                    ctx.invoke_handler(on_action);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "MenuBar", raw);
        }
        return widget;
    }
    if (type == "ContextMenu") {
        auto widget = std::make_unique<tuinator::ContextMenu>();
        tuinator::ContextMenu* raw = widget.get();
        if (const nlohmann::json* items = json::find(node, "items")) {
            std::vector<tuinator::MenuItem> menu_items;
            build_menu_items(*items, menu_items, ctx);
            widget->set_items(std::move(menu_items));
        }
        const std::string look = json::node_string(node, "look", "lookName", "look_name");
        if (!look.empty()) {
            ctx.schedule_post_init([raw, &ctx, look]() { apply_context_menu_look(*raw, look, ctx); });
        }
        const std::string on_action = json::node_string(node, "onAction", "on_action");
        if (!on_action.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_action]() {
                raw->set_on_action([&ctx, on_action](const std::string& /*item*/) {
                    ctx.invoke_handler(on_action);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ContextMenu", raw);
        }
        return widget;
    }
    if (type == "CommandPalette") {
        auto widget = std::make_unique<tuinator::CommandPalette>();
        tuinator::CommandPalette* raw = widget.get();
        if (const nlohmann::json* entries = json::find(node, "entries")) {
            std::vector<tuinator::CommandPaletteEntry> palette_entries;
            for (const auto& entry : *entries) {
                const std::string on_click = json::node_string(entry, "onClick", "on_click");
                palette_entries.push_back(tuinator::CommandPaletteEntry{
                    json::node_string(entry, "id"),
                    json::node_string(entry, "label"),
                    json::node_string(entry, "category"),
                    json::node_string(entry, "shortcut"),
                    [&ctx, on_click]() { if (!on_click.empty()) { ctx.invoke_handler(on_click); } }});
            }
            widget->set_entries(std::move(palette_entries));
        }
        const std::string on_close = json::node_string(node, "onClose", "on_close");
        if (!on_close.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_close]() {
                raw->set_on_close([&ctx, on_close]() { ctx.invoke_handler(on_close); });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "CommandPalette", raw);
        }
        return widget;
    }
    if (type == "StatusBar") {
        const std::string text = json::node_string(node, "text");
        const tuinator::Style style = style::resolve_style(json::sub(node, "style", "muted"), ctx.theme);
        auto widget = std::make_unique<tuinator::StatusBar>(text, style);
        tuinator::StatusBar* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "StatusBar", raw);
        }
        return widget;
    }
    if (type == "StatusLine") {
        auto widget = std::make_unique<tuinator::StatusLine>();
        tuinator::StatusLine* raw = widget.get();
        for (const char* region : {"left", "center", "right"}) {
            const nlohmann::json* segments = nullptr;
            if (std::string(region) == "left") {
                segments = json::find(node, "left", "statusLeft", "status_left");
            } else if (std::string(region) == "center") {
                segments = json::find(node, "center", "statusCenter", "status_center");
            } else {
                segments = json::find(node, "right", "statusRight", "status_right");
            }
            if (!segments || !segments->is_array()) { continue; }
            auto built = build_status_segments(*segments, ctx);
            ctx.schedule_post_init([raw, region = std::string(region), segs = std::move(built)]() mutable {
                if (region == "left") { raw->set_left(std::move(segs)); }
                else if (region == "center") { raw->set_center(std::move(segs)); }
                else { raw->set_right(std::move(segs)); }
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "StatusLine", raw);
        }
        return widget;
    }
    if (type == "ShellTerminal") {
        tuinator::ShellTerminalStyle style_opts{};
        fill_ShellTerminalStyle(style_opts, node, ctx);
        auto widget = std::make_unique<tuinator::ShellTerminal>(style_opts);
        tuinator::ShellTerminal* raw = widget.get();
        const std::string command = json::node_string(node, "command", "shell");
        if (!command.empty()) {
            ctx.schedule_post_init([raw, command]() { raw->start(command); });
        }
        const std::string on_exit = json::node_string(node, "onExit", "on_exit");
        if (!on_exit.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_exit]() {
                raw->set_on_exit([&ctx, on_exit]() { ctx.invoke_handler(on_exit); });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "ShellTerminal", raw);
        }
        return widget;
    }
    if (type == "SourceControlPanel") {
        tuinator::SourceControlPanelStyle style_opts{};
        fill_SourceControlPanelStyle(style_opts, node, ctx);
        auto widget = std::make_unique<tuinator::SourceControlPanel>(style_opts);
        tuinator::SourceControlPanel* raw = widget.get();
        if (const nlohmann::json* header = json::find(node, "header")) {
            ctx.schedule_post_init([raw, header = *header]() {
                raw->set_header(json::node_string(header, "title"), json::node_string(header, "subtitle"));
            });
        }
        if (const nlohmann::json* sections = json::find(node, "sections")) {
            std::vector<tuinator::SourceControlSection> built_sections;
            for (const auto& section : *sections) {
                const std::string title = json::node_string(section, "title");
                const bool expanded = json::node_bool(section, true, "expanded");
                std::vector<tuinator::SourceControlEntry> entries;
                for (const auto& entry : json_array_or_empty(section, "entries")) {
                    const std::string status = json::node_string(entry, "Modified", "status");
                    tuinator::GitChangeStatus status_enum = tuinator::GitChangeStatus::Modified;
                    { const std::string _enum_val = status;
                        if (_enum_val == "Modified") { status_enum = tuinator::GitChangeStatus::Modified; }
                        else if (_enum_val == "Added") { status_enum = tuinator::GitChangeStatus::Added; }
                        else if (_enum_val == "Deleted") { status_enum = tuinator::GitChangeStatus::Deleted; }
                        else if (_enum_val == "Renamed") { status_enum = tuinator::GitChangeStatus::Renamed; }
                        else if (_enum_val == "Copied") { status_enum = tuinator::GitChangeStatus::Copied; }
                        else if (_enum_val == "Untracked") { status_enum = tuinator::GitChangeStatus::Untracked; }
                        else if (_enum_val == "Ignored") { status_enum = tuinator::GitChangeStatus::Ignored; }
                        else if (_enum_val == "Unmerged") { status_enum = tuinator::GitChangeStatus::Unmerged; }
                        else if (_enum_val == "Unknown") { status_enum = tuinator::GitChangeStatus::Unknown; }
                        else { status_enum = tuinator::GitChangeStatus::Modified; }
                    }
                    entries.push_back(tuinator::SourceControlEntry{
                        json::node_string(entry, "path"), status_enum,
                        json::node_string(entry, "additions"), json::node_string(entry, "deletions"),
                        json::node_string(entry, "directoryTag", "directory_tag"),
                        json::node_bool(entry, false, "selected")});
                }
                built_sections.push_back(tuinator::SourceControlSection{title, std::move(entries), expanded});
            }
            ctx.schedule_post_init([raw, sections = std::move(built_sections)]() mutable {
                raw->set_sections(std::move(sections));
            });
        }
        const std::string on_select = json::node_string(node, "onSelect", "on_select");
        if (!on_select.empty()) {
            ctx.schedule_post_init([raw, &ctx, on_select]() {
                raw->set_on_select([&ctx, on_select](
                    int /*section_index*/, int /*entry_index*/, const tuinator::SourceControlEntry& /*entry*/) {
                    ctx.invoke_handler(on_select);
                });
            });
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "SourceControlPanel", raw);
        }
        return widget;
    }
    if (type == "Desktop") {
        auto widget = std::make_unique<tuinator::Desktop>();
        tuinator::Desktop* raw = widget.get();
        if (node.contains("background")) {
            auto background = build_widget(node["background"], ctx);
            widget->set_background(std::move(background));
        }
        create_windows(*raw, "create_window", json_array_or_empty(node, "windows"), ctx, true);
        if (const nlohmann::json* context_menu = json::find(node, "contextMenu", "context_menu")) {
            std::vector<tuinator::MenuItem> items;
            build_menu_items(json_array_or_empty(*context_menu, "items"), items, ctx);
            ctx.schedule_post_init([raw, items = std::move(items)]() mutable {
                raw->set_context_menu_items(std::move(items));
            });
        }
        if (ctx.actions) {
            ctx.schedule_post_init([raw, actions = ctx.actions]() { raw->set_action_registry(actions); });
        }
        if (const nlohmann::json* palette = json::find(node, "commandPalette", "command_palette")) {
            if (json::node_bool(*palette, false, "fromActions", "from_actions") && ctx.actions) {
                ctx.schedule_post_init([raw, actions = ctx.actions]() {
                    raw->set_action_registry(actions);
                    std::vector<tuinator::CommandPaletteEntry> entries;
                    for (const tuinator::RegisteredAction* action : actions->all()) {
                        entries.push_back(tuinator::CommandPaletteEntry{
                            action->id, action->label, action->category, action->shortcut, action->callback});
                    }
                    raw->set_command_palette_entries(std::move(entries));
                });
            } else if (palette->contains("entries")) {
                std::vector<tuinator::CommandPaletteEntry> entries;
                for (const auto& entry : (*palette)["entries"]) {
                    const std::string on_click = json::node_string(entry, "onClick", "on_click");
                    entries.push_back(tuinator::CommandPaletteEntry{
                        json::node_string(entry, "id"), json::node_string(entry, "label"),
                        json::node_string(entry, "category"), json::node_string(entry, "shortcut"),
                        [&ctx, on_click]() { if (!on_click.empty()) { ctx.invoke_handler(on_click); } }});
                }
                ctx.schedule_post_init([raw, entries = std::move(entries)]() mutable {
                    raw->set_command_palette_entries(std::move(entries));
                });
            }
        }
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Desktop", raw);
        }
        return widget;
    }
    if (type == "WindowHost") {
        auto widget = std::make_unique<tuinator::WindowHost>();
        tuinator::WindowHost* raw = widget.get();
        create_windows(*widget, "create_window", json_array_or_empty(node, "windows"), ctx, false);
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "WindowHost", raw);
        }
        return widget;
    }
    if (type == "MessageDialog") {
        const std::string message = json::node_string(node, "message", "text");
        const std::string on_ok = json::node_string(node, "onOk", "on_ok", "onClick", "on_click");
        auto widget = tuinator::dialog::make_message(ctx.theme, message, [&ctx, on_ok]() {
            if (!on_ok.empty()) { ctx.invoke_handler(on_ok); }
        });
        tuinator::Widget* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Widget", raw);
        }
        return widget;
    }
    if (type == "ConfirmDialog") {
        const std::string message = json::node_string(node, "message", "text");
        const std::string on_result = json::node_string(node, "onResult", "on_result", "onConfirm", "on_confirm");
        auto widget = tuinator::dialog::make_confirm(ctx.theme, message, [&ctx, on_result](bool /*confirmed*/) {
            if (!on_result.empty()) { ctx.invoke_handler(on_result); }
        });
        tuinator::Widget* raw = widget.get();
        if (const std::string widget_id = json::node_string(node, "id"); !widget_id.empty()) {
            ctx.register_widget(widget_id, "Widget", raw);
        }
        return widget;
    }

    throw std::runtime_error("Unsupported widget type: " + type);
}

} // namespace tuinator::scene::detail
